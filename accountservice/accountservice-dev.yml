security:
  oauth2:
    client:
      clientId: account-service
      clientSecret: thisissecret
      accessTokenUri: http://auth/uaa/oauth/token
      grant-type: client_credentials
      scope: server

server:
  servlet:
    context-path: /accounts
  port: 8082

feign:
  hystrix:
    enabled: true
