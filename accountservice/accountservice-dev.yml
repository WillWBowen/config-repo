security:
  oauth2:
    client:
      clientId: account-service
      clientSecret: thisissecret
      accessTokenUri: http://auth:8901/uaa/oauth/token
      grant-type: client_credentials
      scope: server

server:
  port: 8082

feign:
  hystrix:
    enabled: true
