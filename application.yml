eureka:
  instance:
    prefer-ip-address: false
  client:
    serviceUrl:
      defaultZone: http://eurekaserver/eureka/

security:
  oauth2:
    resource:
      user-info-uri: http://authenticationservice/uaa/users/current
      token-info-uri: http://authenticationservice/uaa/oauth/check_token

logging:
  level:
    org.springframework.security: DEBUG

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000
