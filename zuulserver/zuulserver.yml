zuul:
  ignored-services: '*'
  prefix: /api
  routes:
    customerservice: /customer/**
    salonservice: /salon/**
  debug:
    request: true
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 2500
